/**
 * Marker utility for files that require project-specific customization.
 * Grep for `@customization-required` to find all placeholder files.
 */
export function customizationRequired(area: string): void {
  void area;
}
